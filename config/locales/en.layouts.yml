en:
  layouts:
    shared:
      nav:
        sign_in: Sign in
        sign_out: Sign out
        register: Sign up
        minimum_password_length: "minimum %{length} symbols"
        new_post: Create post
      footer:
        source_code: Source code
